public enum ControllerState
{
    territorySelection, //No Territory is currently selected
    actionSelection, //Territory selected, no action currently selected
    targetSelection, //Action is selected, no target is currenttly selected
}